sudo: false
services:
  - docker
before_install:
  - docker --version
  - docker pull vivekratnavel/hadoop-ozone-build:v1
  - docker ps -a
script:
  - docker run vivekratnavel/hadoop-ozone-build:v1 -v .:/home/user /bin/sh -c "cd /home/user && ./hadoop-ozone/dev-support/checks/integration.sh"
